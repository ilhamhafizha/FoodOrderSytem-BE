server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://ep-wild-glitter-a1pwyurp-pooler.ap-southeast-1.aws.neon.tech:5432/food_order_system?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_0UVhdw8LZaut
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        enable_lazy_load_no_trans: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  profiles:
    active: development
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  upload-directory: fos-uploads
  folder-temp: ./fos-temp/
